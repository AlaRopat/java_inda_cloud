server:
  port: 8081

spring:
  sleuth:
    sampler:
      probability: 100
  zipkin:
    baseUrl: http://localhost:9411/
  application:
    name: catalog

feign:
  hystrix:
    enabled: true

logging:
  level:
    org.springframework.web.reactive.function.server.RouterFunctions: DEBUG

